# PantryCook v0.1
# Web UI:  http://localhost:3000
# API:     http://localhost:8000
# Health:  http://localhost:8000/health

name: pantrycook

services:
  api:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: ${DATABASE_URL:-sqlite:////data/pantrycook.db}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_BASE_URL: ${OPENAI_BASE_URL:-https://api.openai.com/v1}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-4o-mini}
      CORS_ORIGINS: ${CORS_ORIGINS:-*}
    volumes:
      - pantrycook-data:/data
    restart: unless-stopped

  web:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped

volumes:
  pantrycook-data:

# Optional Postgres
# Swap DATABASE_URL to postgresql+psycopg://pantrycook:pantrycook@db:5432/pantrycook
# and add:
#
#  db:
#    image: postgres:16-alpine
#    environment:
#      POSTGRES_USER: pantrycook
#      POSTGRES_PASSWORD: pantrycook
#      POSTGRES_DB: pantrycook
#    volumes:
#      - pantrycook-pg:/var/lib/postgresql/data
#    restart: unless-stopped
#
# volumes:
#   pantrycook-pg:
